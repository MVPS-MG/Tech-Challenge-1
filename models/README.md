# models/

Artefatos do modelo campeão treinado (Etapa 2), salvos como `.pkl` ou `.joblib`,
para serem carregados pela API de inferência (Etapa 3).

Não versionar múltiplos modelos experimentais aqui — apenas o modelo final
escolhido e documentado no Model Card (`docs/model_card.md`).
