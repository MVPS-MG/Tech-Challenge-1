# tests/

Testes automatizados com `pytest`, cobrindo no mínimo:

- uma função de pré-processamento (ex.: imputação ou encoding em `src/preprocessing.py`);
- o status da API (`GET /health` retornando 200).

Rodar com:

```bash
pytest
```
